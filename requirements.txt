# Core (required to run the app, locally and on Streamlit Community Cloud)
streamlit>=1.32
opencv-python-headless>=4.8,<5
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
altair>=5.0
pytest>=7.0

# Optional — high-accuracy backend (dlib-based, per PRD Sec. 8).
# The app works WITHOUT this via the built-in OpenCV fallback backend.
# Install only on Python 3.10-3.11 for best compatibility:
#   pip install face_recognition
# face_recognition>=1.3.0
